{
  "name": "brume",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "pnpm": {
    "overrides": {
      "react-refresh": "0.17.0"
    }
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
